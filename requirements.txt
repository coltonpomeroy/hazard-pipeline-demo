# Core scientific / geospatial stack for the hazard pipeline.
# Versions are what this demo was built and tested against; ranges are fine.
xarray>=2024.0
zarr>=3.0
netCDF4>=1.6
cfgrib>=0.9.12
eccodes>=2.40
rasterio>=1.3
requests>=2.31
shapely>=2.0
numpy>=1.26
matplotlib>=3.8
dask>=2024.0
